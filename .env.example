# Local Development Environment Variables
# Copy this file to .env and adjust values as needed

# PostgreSQL Configuration (optional for local dev - uses defaults if not set)
POSTGRES_DB=lnf
POSTGRES_USER=lnf
POSTGRES_PASSWORD=lnf_password
